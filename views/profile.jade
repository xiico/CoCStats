extends layout

block content
  div
    div(class="well")
      h3 #[span(class="fa fa-user")] Local
      strong id: #[=user._id] #[br]
      strong email: #[=user.local.email] #[br]
      strong password: #[=user.local.password] #[br]
      strong url: #[=url] #[br]